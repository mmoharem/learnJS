













6- Adding new item to the UI.
    1- A technique for adding big chunks of html into DOM.
    2- How to replace parts of strings.
    3- How to do DOM manipulation using the insertAdjacentHTML method.

7- Clearing HTML fiels.
    1- How to clear HTML fieds.
    2- How to use querySelectorAll.
    3- How to convert a list to an array.
    4- A bette way to loop over an array then for loops: for-each

            var fields, fieldsArr;
            fields = document.querySelectorAll(domStrings.inputDescr + ', ' + domStrings.inputValue);
            fieldsArr = Array.prototype.slice.call(fields);
            //using foreach(callback fn)
            fieldsArr.forEach(function(current, index, array) {

                current.value = "";
            });

            fieldsArr[0].focus();

8- Updating Budget.
    1- How to convert field inputs to numbers;
        parseFloat(document.querySelector(domStrings.inputValue).value)
    2- How to prevent false input.
        if (input.description !== "" && !isNaN(input.value) && input.value > 0) {
            
        }

9- Updating Budget-2.
    1- How and why to create simple, reusable functions with only one purpose;
    2- How to sum all elements of an array using the forEAch method.
    3- Practice DOM manipulation by updating the buget and total values. 

10- Setting up the Delete event using event delegation
    1- How to use event delegation in practive.
    2- How to use IDs in HTML to connect the UI with the data model.
    3- How to use parentNode property for DOM traversing.
    4- More DOM manipulation: removing item from the DOM.

11- Update and Display Expense Percentage
    1-
    2- How to create our own forEach function but for nodeLists instead of arrays.


12- Formatting numbers 
    1. adding (+ or -)
    2. add 2 decimal points
    3. add comma seprating thousends